use insane_ai_security as security;

fn main() {
    // Minimal analyzer stub
    println!("Threat analyzer starting...");
    let _cfg = security::SystemConfig::default();
    println!("Analyzer initialized with level: {:?}", _cfg.intelligence_level);
}
